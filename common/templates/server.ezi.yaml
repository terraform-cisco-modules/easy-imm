---
#=======================================
#   Templates -> Server - Variables
#=======================================
common: # Name of the Organization
  templates:
    server:
      - bios_policy: M5-intel-virtual
        boot_order_policy: M2-pxe
        create_template: true
        description: aci-M5-intel-virtual-M2-pxe Server Profile Template
        firmware_policy: fw-domain-servers
        imc_access_policy: aci-kvm
        ipmi_over_lan_policy: ipmi
        lan_connectivity_policy: lcp
        local_user_policy: users
        name: aci-M5-intel-virtual-M2-pxe
        power_policy: server
        san_connectivity_policy: scp
        serial_over_lan_policy: sol
        snmp_policy: snmp
        storage_policy: M2-Raid
        target_platform: FIAttached
        thermal_policy: server
        uuid_pool: uuid
        virtual_kvm_policy: vkvm
        virtual_media_policy: vmedia
      - bios_policy: M5-intel-virtual-tpm
        boot_order_policy: M2-pxe
        create_template: true
        description: aci-M5-intel-virtual-tpm-M2-pxe Server Profile Template
        firmware_policy: fw-domain-servers
        imc_access_policy: aci-kvm
        ipmi_over_lan_policy: ipmi
        lan_connectivity_policy: lcp
        local_user_policy: users
        name: aci-M5-intel-virtual-tpm-M2-pxe
        power_policy: server
        san_connectivity_policy: scp
        serial_over_lan_policy: sol
        snmp_policy: snmp
        storage_policy: M2-Raid
        target_platform: FIAttached
        thermal_policy: server
        uuid_pool: uuid
        virtual_kvm_policy: vkvm
        virtual_media_policy: vmedia
      - bios_policy: M6-intel-virtual-tpm
        boot_order_policy: M2-pxe
        create_template: true
        description: aci-M6-intel-virtual-tpm-M2-pxe Server Profile Template
        firmware_policy: fw-domain-servers
        imc_access_policy: aci-kvm
        ipmi_over_lan_policy: ipmi
        lan_connectivity_policy: lcp
        local_user_policy: users
        name: aci-M6-intel-virtual-tpm-M2-pxe
        power_policy: server
        san_connectivity_policy: scp
        serial_over_lan_policy: sol
        snmp_policy: snmp
        storage_policy: M2-Raid
        target_platform: FIAttached
        thermal_policy: server
        uuid_pool: uuid
        virtual_kvm_policy: vkvm
        virtual_media_policy: vmedia
